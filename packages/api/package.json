{
  "name": "setmaster-api",
  "version": "1.0.0",
  "scripts": {
    "lint:ts": "pnpm tsc --noEmit",
    "biome:fix": "cd ../../ && pnpm run biome:fix",
    "fix": "pnpm biome:fix && pnpm lint:ts",
    "start": "npx ts-node -r tsconfig-paths/register ./src/app.ts",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@types/node": "^22.13.11",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
