{
  "name": "setmaster-api",
  "version": "1.0.0",
  "scripts": {
    "lint:ts": "bun tsc --noEmit",
    "biome:fix": "cd ../../ && bun run biome:fix",
    "fix": "bun biome:fix && bun lint:ts",
    "start": "bun run ./src/app.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.2.7"
  },
  "private": true
}